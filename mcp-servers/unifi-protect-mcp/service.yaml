apiVersion: v1
kind: Service
metadata:
  name: unifi-protect-mcp
  namespace: mcp-servers
  labels:
    app: unifi-protect-mcp
    component: mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: unifi-protect-mcp
