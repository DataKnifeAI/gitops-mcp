apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana-mcp
  namespace: mcp-servers
  labels:
    app: grafana-mcp
    component: mcp-server
    managed-by: gitops
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  rules:
  - host: grafana-mcp.dataknife.net
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: grafana-mcp
            port:
              number: 8000
