apiVersion: apps/v1
kind: Deployment
metadata:
  name: unifi-network-mcp
  namespace: mcp-servers
  labels:
    app: unifi-network-mcp
    version: v1
    component: mcp-server
spec:
  replicas: 3
  selector:
    matchLabels:
      app: unifi-network-mcp
  template:
    metadata:
      labels:
        app: unifi-network-mcp
        component: mcp-server
    spec:
      imagePullSecrets:
      - name: harbor-registry-secret
      containers:
      - name: unifi-network-mcp
        image: harbor.dataknife.net/library/unifi-network-mcp:v0.10
        imagePullPolicy: IfNotPresent
        
        # HTTP Transport Configuration
        env:
        - name: MCP_TRANSPORT
          value: "http"
        - name: MCP_HTTP_ADDR
          value: ":8000"
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: unifi-network-mcp-config
              key: LOG_LEVEL
              optional: true
        - name: UNIFI_BASE_URL
          valueFrom:
            configMapKeyRef:
              name: unifi-network-mcp-config
              key: UNIFI_BASE_URL
              optional: true
        - name: UNIFI_API_KEY
          valueFrom:
            secretKeyRef:
              name: unifi-network-mcp-secrets
              key: UNIFI_API_KEY
        - name: UNIFI_SKIP_SSL_VERIFY
          valueFrom:
            configMapKeyRef:
              name: unifi-network-mcp-config
              key: UNIFI_SKIP_SSL_VERIFY
              optional: true
        
        ports:
        - name: http
          containerPort: 8000
          protocol: TCP
        
        # Health Checks
        livenessProbe:
          httpGet:
            path: /health
            port: http
            scheme: HTTP
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 5
          successThreshold: 1
          failureThreshold: 3
        
        readinessProbe:
          httpGet:
            path: /health
            port: http
            scheme: HTTP
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 3
          successThreshold: 1
          failureThreshold: 2
        
        # Resource Management
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        
        # Security Context
        securityContext:
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 1000
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
      
      # Pod Disruption Budget
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - unifi-network-mcp
              topologyKey: kubernetes.io/hostname
