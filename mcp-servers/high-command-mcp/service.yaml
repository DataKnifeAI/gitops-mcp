apiVersion: v1
kind: Service
metadata:
  name: high-command-mcp
  namespace: mcp-servers
  labels:
    app: high-command-mcp
    component: mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: high-command-mcp
