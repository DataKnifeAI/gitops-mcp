apiVersion: v1
kind: Service
metadata:
  name: unifi-manager-mcp
  namespace: mcp-servers
  labels:
    app: unifi-manager-mcp
    component: mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: unifi-manager-mcp
