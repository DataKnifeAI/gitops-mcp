apiVersion: v1
kind: Service
metadata:
  name: proxmox-ve-mcp
  namespace: mcp-servers
  labels:
    app: proxmox-ve-mcp
    component: mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: proxmox-ve-mcp
